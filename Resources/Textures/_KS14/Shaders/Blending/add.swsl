
// Fed by CopyToShaderParameters
uniform highp vec2 DST_UV;
uniform sampler2D DST_TEXTURE;

void fragment()
{
    //lowp vec4 base = texture(TEXTURE, UV);
    //lowp vec4 add  = texture(DST_TEXTURE, DST_UV);

    //lowp vec3 result = base.rgb + add.rgb;

    //COLOR.rgb = min(result, vec3(1.0));
    //COLOR.a = max(base.a, add.a);
    lowp vec4 add  = texture(DST_TEXTURE, DST_UV);
    COLOR = add
}
